{
  "name": "server",
  "version": "1.0.0",
  "description": "Server package",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified for server\" && exit 1"
  },
  "dependencies": {
    "shared": "workspace:*"
  }
}
