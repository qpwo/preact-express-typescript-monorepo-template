{
  "name": "server",
  "version": "1.0.0",
  "description": "Server package",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "start": "esr src/server.ts",
    "watch": "nodemon --watch 'src/**/*' -e ts,json --exec 'esr src/server.ts'"
  },
  "dependencies": {
    "@types/express": "^5.0.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "nodemon": "^3.1.9",
    "shared": "workspace:*"
  }
}
